def depth_first_search(G,v):
  '''
  input: G = (V, E) , and v as a vertex 
  output : depends...
	  total vertices
	  total visitied vertices
  '''

def dfs_numbering(G, v):
  '''
  output : for every vertex v, v.DFS is set to the DFS number of v.
  '''
  return G
  
def build_DFS_tree(G, v):
  '''
  given a graph
  output: a DFS tree
  '''
  return 

def depth_first_search_nonrecursive(G, v) :

  return G
