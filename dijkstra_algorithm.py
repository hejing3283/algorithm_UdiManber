def single_source_shortest_path(G, v):
  '''
  find the shrotest path of a graph
  input: G = (V,E) a weighted directed graph, and v ( the source vertex) 
  output: for each vertex w, w.SP is the length of shortest path from v to w
  '''
  return 
